namespace miterm {
  
  export declare namespace httpServer {
    type Options = {
      port: number;
    }
  }
  
  export function httpServer(options: httpServer.Options): Promise<httpServer> {
    
    //var s = httpServer(null);
  }
}